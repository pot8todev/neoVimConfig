require("fzf-lua").setup({
  fzf_bin = "fzf", -- (normalmente não precisa mexer)
  fzf_native = true, -- ativa a extensão nativa se instalada
  profile = "fzf-native", -- aqui você realmente escolhe o perfil
})
